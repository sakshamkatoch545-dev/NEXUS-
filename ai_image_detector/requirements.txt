numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
Pillow>=10.0
joblib>=1.3
ImageHash>=4.3

# Optional but recommended: enables the deep-embedding branch (CLIP).
# The detector degrades gracefully to forensic-only mode if these are
# missing or if model weights can't be downloaded.
torch>=2.0
transformers>=4.35
